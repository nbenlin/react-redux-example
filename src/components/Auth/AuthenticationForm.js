import React from "react";
import Card from "../UI/Card/Card";
import { Form } from "./AuthenticationForm.styled";

const AuthenticationForm = () => {
  return (
    <Card>
      <Form>AuthenticationForm</Form>
    </Card>
  );
};

export default AuthenticationForm;
